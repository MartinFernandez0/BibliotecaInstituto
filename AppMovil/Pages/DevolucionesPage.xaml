<?xml version="1.0" encoding="utf-8" ?>
    <ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:AppMovil.ViewModels"
             xmlns:pages="clr-namespace:AppMovil.Pages"
             x:Class="AppMovil.Pages.DevolucionesPage"
             Title="Devoluciones">
    <ContentPage.BindingContext>
        <viewModels:DevolucionesViewModel />
    </ContentPage.BindingContext>
    <StackLayout Padding="20">
        <Label Text="Mis Devoluciones" FontSize="24" HorizontalOptions="Center"/>
        <CollectionView ItemsSource="{Binding Devoluciones}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Label Text="{Binding .}"/>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </StackLayout>
</ContentPage>